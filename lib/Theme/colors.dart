import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors - KFC Brand
  static const Color primary = Color(0xFFE4002B);        // KFC Red chính
  static const Color primaryLight = Color(0xFFFF5722);   // Red nhạt
  static const Color primaryDark = Color(0xFFB71C1C);    // Red đậm
  
  // Secondary Colors
  static const Color secondary = Color(0xFFFFC107);      // Vàng KFC
  static const Color secondaryLight = Color(0xFFFFEB3B); // Vàng nhạt
  static const Color secondaryDark = Color(0xFFFF8F00);  // Vàng đậm
  
  // Neutral Colors
  static const Color background = Color(0xFFF8F9FA);     // Nền sáng
  static const Color surface = Color(0xFFFFFFFF);        // Surface trắng
  static const Color surfaceVariant = Color(0xFFF5F5F5); // Surface xám nhạt
  
  // Text Colors
  static const Color textPrimary = Color(0xFF212121);    // Text chính
  static const Color textSecondary = Color(0xFF757575);  // Text phụ
  static const Color textOnPrimary = Color(0xFFFFFFFF);  // Text trên nền đỏ
  
  // Status Colors
  static const Color success = Color(0xFF4CAF50);        // Xanh lá
  static const Color warning = Color(0xFFFF9800);        // Cam
  static const Color error = Color(0xFFF44336);          // Đỏ lỗi
  static const Color info = Color(0xFF2196F3);

  static var outline;           // Xanh dương
}
